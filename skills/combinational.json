{
  "version": "2.0",
  "skills": [
    {
      "id": "comb_curated_001",
      "category": "combinational",
      "error_type": "LOGIC",
      "pattern": "XOR operation produces XNOR result \u2014 output inverted",
      "fix": "Change ^ operator back to ^, remove any ~ wrapping the XOR expression",
      "example": "Incorrect: assign Y = ~(A ^ B); Correct: assign Y = A ^ B;",
      "confirmed_count": 10,
      "success_count": 10,
      "curated": true,
      "last_seen": "alu_8bit"
    },
    {
      "id": "comb_curated_002",
      "category": "combinational",
      "error_type": "LOGIC",
      "pattern": "zero_flag not updating \u2014 missing from always block or wrong condition",
      "fix": "Add zero_flag = (Y == 0) inside the same always block as output Y, after Y is computed",
      "example": "always @(*) begin Y = A + B; zero_flag = (Y == 8'b0); end",
      "confirmed_count": 8,
      "success_count": 8,
      "curated": true,
      "last_seen": "alu_8bit"
    },
    {
      "id": "combinational_0016",
      "category": "combinational",
      "error_type": "LOGIC",
      "pattern": "The zero_flag is not being asserted for opcode 5 (3'b101, NOT operation) when both inputs are zero because the RTL computes ~A which for A=0 becomes 8'hFF, which is non-zero.",
      "fix": "Either update the test to expect zero_flag=0 for NOT operation with zero inputs (since ~0 = 255, which is non-zero), or if zero_flag should be 1 for all operations with zero results, verify the RTL's NOT behavior is as intended.",
      "design_name": "alu_8bit",
      "success_count": 1,
      "confirmed_count": 1,
      "curated": false,
      "last_seen": "alu_8bit"
    },
    {
      "id": "combinational_0050",
      "category": "combinational",
      "error_type": "LOGIC",
      "pattern": "The XOR operation (opcode 3'b100) incorrectly computes bitwise XNOR (~(A ^ B)) instead of bitwise XOR (A ^ B).",
      "fix": "Replace the XNOR operator with XOR by removing the bitwise NOT from the XOR operation for opcode 100.",
      "design_name": "alu_8bit",
      "success_count": 1,
      "confirmed_count": 1,
      "curated": false,
      "last_seen": "alu_8bit"
    },
    {
      "id": "combinational_0052",
      "category": "combinational",
      "error_type": "LOGIC",
      "pattern": "The zero_flag output register is declared but never assigned in the always block, leaving it in an unknown 'X' state.",
      "fix": "Assign the zero_flag output based on whether Y is zero, matching the reference model where zero_flag = 1 if Y == 0 else 0.",
      "design_name": "alu_8bit",
      "success_count": 1,
      "confirmed_count": 1,
      "curated": false,
      "last_seen": "alu_8bit"
    }
  ]
}